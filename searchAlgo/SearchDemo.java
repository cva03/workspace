package searchAlgo;import javax.sound.sampled.Line;import java.util.Scanner;public class SearchDemo {    public static void main(String[] args) {        Scanner sc=new Scanner(System.in);        int n=sc.nextInt();        int[] arr= new int[n];        for(int i=0;i<n;i++){            arr[i]=sc.nextInt();        }        System.out.println("Enter the number you want to search");        int num=sc.nextInt();        int index=Integer.MAX_VALUE;        // index=LinearSearch.find(arr,num);        //index= LinearSearch.recurFind(arr,arr.length,num);        //index=BinarySearch.find(arr,num);        //index=BinarySearch.recurFind(arr,num);        System.out.println("index = "+index);        System.out.println((char)3021);    }}